# Production Environment Variables
GIN_MODE=release
PORT=8080
HOST=0.0.0.0

# Database (use connection string with credentials)
MONGODB_URI=mongodb+srv://rudransh-srivastava:rudransh-sri@cluster0.nbybrcl.mongodb.net/
DATABASE_NAME=real_time_monitoring_tool

# CORS (your production frontend URLs)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
FRONTEND_URL=https://yourdomain.com

# Server Timeouts
READ_TIMEOUT=30
WRITE_TIMEOUT=30
IDLE_TIMEOUT=120

# Monitoring
MAX_CONCURRENT_CHECKS=200
METRICS_RETENTION_DAYS=90

# Security (optional HTTPS)
ENABLE_HTTPS=false
# CERT_FILE=/path/to/cert.pem
# KEY_FILE=/path/to/key.pem

# Trusted Proxies (if behind nginx/load balancer)
TRUSTED_PROXIES=10.0.0.0/8